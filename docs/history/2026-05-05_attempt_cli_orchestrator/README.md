# Attempt: CLI 기반 Denavy 오케스트레이터 구현

## 목적
- 설계 → 구현 단계 자동 변환 시스템 구축
- LLM을 통제 가능한 하네스로 감싸서 결정론적 실행 구조 만들기

## 범위
- 포함된 것:
  - CLI 인터페이스 (denavy run)
  - 오케스트레이터 (PRPAO 루프)
  - RC 기반 방어 모듈 구조 설계
- 제외한 것:
  - 실제 코드 수정 자동화
  - 완전한 end-to-end 실행

## 핵심 아이디어
- 설계를 그대로 실행하지 않고
  → 검증 가능한 단계로 분해 후 실행
- LLM을 직접 쓰지 않고
  → 구조화 + 검증 레이어로 감싸기

## 결과
- 성공 여부: 실패
- 종료 시점 상태:
  - 어디까지 구현됨:
    - CLI 구조
    - 오케스트레이터 틀
    - 일부 RC 모듈 구조
  - 무엇이 동작했음:
    - 명령 실행 흐름
    - 구조적 설계는 존재
  - 무엇이 안 됐음:
    - 실제 유의미한 결과 생성
    - “쓸 수 있는 도구” 단계 도달

## 한 줄 요약
- 구조는 만들었지만, 실제로 쓸 수 있는 최소 단위까지 못 내려갔다
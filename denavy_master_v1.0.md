# 🧭 [Master Blueprint] Denavy 아키텍처: Spec-First 형태 언어 및 런타임 명세서 v1.0

> **[System Context for AI Agents]**
> 본 문서는 자율형 AI 코딩 에이전트를 위한 3대 오케스트레이션 축(`Registry`, `Denavy`, `Hwatotbul`) 중, **'공간(디렉토리 트리)과 불변의 법칙(제약/형태언어/런타임)'**을 관장하는 입법부, `Denavy`에 대한 단일 통합 지식 베이스이다. 에이전트는 본 문서를 바탕으로 코드 생성의 물리적 제약, **추상 논리 추출(ALE) 기반의 역방향 결재 메커니즘**, 런타임 무결성 환경을 완벽히 인지해야 한다.

## 0. 아키텍처 진화의 역사 및 애자일 철학 (Historical Context)

에이전트는 이 시스템이 어떠한 고민과 시행착오를 거쳐 현재에 이르렀는지 반드시 인지해야 한다.

* **과거의 환상 (Monolithic & Auto-Healing):**
* 초기에는 Denavy가 에이전트의 상태, 칸반, 트라우마뿐만 아니라 현재 분리된 `Registry`를 포함하여 지면에 일일이 나열할 수조차 없을 만큼 수많은 역할들을 모두 포괄하는 거대한 통합 운영체제로 구상되었다.
* "에이전트가 논리적 허점을 발견하면 스스로 명세를 뜯어고친다"는 자율 치유(Self-Healing)의 환상은 에이전트의 환각을 방치하는 철저한 통제력 상실임을 깨닫고 전면 폐기했다.

* **역방향 동기화(RLS)의 진화 - '무지성 병합'에서 '연옥 결재'로:**
* "인간이 코드를 고치면 시스템이 알아서 `src ➔ spec ➔ dna` 순으로 명세를 덮어씌워 준다"는 것은 우리가 반드시 달성해야 할 궁극의 역방향 선형 동기화(RLS) 로망이다.
* 그러나 역추론 과정에서의 AI 환각 위험, 그리고 지엽적인 소스 코드(`src`) 수정이 전역 헌법(`dna`)을 무의식적으로 오염시키는 1:N:M 피라미드 붕괴를 방치할 수는 없었다. 따라서 이를 무지성 자동 병합으로 두는 대신, **ALE(추상 논리 추출기)가 역방향 변경안(Draft)만 추출하여 대기시키고, 최종 병합은 반드시 인간의 승인(Approve)을 거치도록 강제하는 '연옥(Purgatory) 결재 메커니즘'으로 안전하게 타협**하고 진화시켰다.

* **현재의 패러다임 (Decoupled & Human-Centric):**
* 수많은 시행착오 끝에 시스템을 3개의 독립된 축으로 철저히 분리했다.

1. **Hwatotbul (시간과 기억):** 트라우마를 제어하는 단일 닻(Anchor) 파일과, 그에 종속된 비동기 칸반 작업장(`Bonghwa`)을 통해 에이전트의 일정을 통제한다.
2. **Registry (명부):** 거대했던 Denavy에서 떼어내어 현재 시점에서는 억지로 역할을 부여하지 않고 보류(Pending)하되, 향후 에이전트 전역 심볼이나 외부 플러그인 등록소 등으로 진화할 여지를 둔다.
3. **Denavy (공간과 법칙):** 본 문서는 오직 '어떤 공간에서, 어떤 언어로 **명세를 기안(Draft) 및 결재(Approve)받고, 런타임에서 증명할 것인가**'에만 집중한다.

* **애자일 진화 선언:**
* 현재 정의된 `/dna`와 `/spec`만으로 백엔드 로직 외에 UI/UX나 클라우드 인프라(IaC) 같은 미지의 영역들을 완벽히 담아낼 수 있을지에 대한 근원적 의심을 상시 유지한다.
* 특히 **프론트엔드 명세의 완벽한 편입은 앞으로 직접 부딪히며 풀어나가야 할 핵심 과제**이다. 알지 못하는 영역까지 억지로 한 번에 완벽히 설계하려 기하기보다, 실제 코딩을 진행하며 한계에 부딪힐 때마다 버전을 업데이트하며 진화시키는 애자일(Agile) 방식을 따른다.

---

## 1. 아키텍처 핵심 철학: Spec-First Paradigm

**"명세가 곧 컴퓨터이다. 그리고 명세의 통제권은 오직 인간에게 있다."**

자연어 프롬프트에 의존해 즉흥적으로 코드를 짜는 에이전트의 행위는 유지보수 불가능한 스파게티 코드를 낳는다.

* **코드의 격하:** 실제 소스 코드(`/src`)는 영구적 자산이 아니다. 언제든 지우고 다시 생성할 수 있는 일회성(Ephemeral) 컴파일 찌꺼기다.
* **명세의 격상:** 시스템의 불변하는 본질과 논리적 흐름은 오직 인간 아키텍트가 결재한 **명세(Spec)** 안에만 존재한다. 에이전트는 이 명세를 완벽히 통과할 때만 코드를 배포할 수 있다.
* **기안(Draft)과 결재(Approve)의 분리:** 에이전트는 지시를 받으면 즉시 코드를 짜는 것이 아니라, 반드시 압축된 기호 언어로 시스템 3대 명세(DNA/SPEC/TEST) 초안을 스스로 **'기안'**하여 보고할 의무를 진다. 인간 아키텍트의 **'결재'**가 떨어진 후에야 비로소 샌드박스에서 타겟 언어(`/src`)로 코드를 구현할 권한을 얻는다.

---

## 2. 공간의 통제: 도메인 기반 1:N:M 피라미드 미러링

프로젝트의 공간은 비즈니스 도메인(예: `auth`, `payment`)을 기준으로 3개의 물리적 최상위 폴더에 엄격히 격리된다. 이는 정적인 뼈대(데이터)와 동적인 흐름(로직)을 섞었을 때 발생하는 LLM 컨텍스트 오염을 막기 위함이다.

주의할 점은, 세 공간 내부의 파일 개수가 맹목적인 1:1:1로 매핑되는 것이 아니다. 도메인 단위의 폴더 구조는 평행하게 미러링되지만, 실제 파일은 **`1(DNA) : N(SPEC/TEST) : M(SRC)`의 피라미드 투영(Projection) 구조**를 띤다. 특정 도메인의 전역 뼈대(`.dna`) 하나가 수많은 개별 사건 명세(`.spec`)와 프레임워크 구현체(`.src`)를 파생시키는 구조다. 이를 통해 에이전트는 특정 로직을 작업할 때, 무거운 전체 시스템이 아닌 '해당 도메인의 뼈대(1)'와 '해당 유스케이스 명세(1)'만 O(1)로 적시 로딩(Just-in-Time)하여 컨텍스트 창을 극도로 가볍게 유지한다.

### 2.1. `/dna` (정적 뼈대 및 절대 제약 / 입법부 - "명사와 상태")

시스템의 데이터 뼈대(Entity 구조, 타입)와 시공간을 초월하여 절대 위반해선 안 되는 글로벌 보안/비즈니스 제약(Policy)만 정의한다. 시간의 흐름(순서)이나 제어 로직(`if`, `for`)은 일절 들어가지 않는 철저한 Read-Only 영역이다.

* **계층적 오버라이딩 (스코프 체인):** 최상위 헌법인 `/dna/root.dna`가 시스템 전체의 기본 제약을 깔고, 하위 도메인 폴더(예: `/dna/auth/auth.dna`)로 깊이 들어갈수록 **'가장 가까운 로컬 뼈대(Local Policy)'가 전역 뼈대를 덮어쓰거나(Override) 우선**하는 특별법 우선의 원칙을 따른다.

### 2.2. `/spec` (운영 및 검증 명세 계층 / 행정·사법부 - "동사와 흐름")

`/dna`의 뼈대를 참조하여, 시간의 흐름에 따른 조건 분기, 함수 실행 등의 제어 흐름(Logic)을 기호로 정의하는 '설계도'다. 인간이 결재하고 에이전트가 코드를 짤 때 쳐다보는 유일한 진실의 원천이다.

* **사법부의 하위 폴더 분리 (`/spec/test/`):** 파일이 비대해지는 것을 막고 타겟 언어(Java 등)의 테스트 분리 관례를 수용하기 위해, 로직 명세와 검증 채점표(Proof)는 물리적으로 분리한다. 에이전트는 `/spec/auth.spec`에 로직을 기안하고, 이를 검증하는 테스트 케이스는 `/spec/test/auth_test.spec`에 별도로 작성하여 기안한다.

### 2.3. `/src` (구현체 계층 / 결과물)

위 명세들을 바탕으로 에이전트가 기계적으로 찍어낸 타겟 언어 소스 코드. 인간은 이 폴더를 직접 수정하지 않는 것을 원칙으로 한다. 에이전트는 명세(`/spec` 및 `/spec/test/`)를 해석하여, 타겟 언어의 디렉토리 관례(예: Java의 `src/main` 및 `src/test`)에 맞추어 자율적으로 메인 코드와 테스트 코드를 분리 생성할 책임(Transpiling)을 진다.

---

## 3. 언어의 통제: 독자적인 '형태 언어 (Morphological Language)'

LLM에게 JSON이나 YAML은 괄호(`{}`)와 따옴표(`""`)가 난무하는 **'구문적 노이즈(Syntactic Noise)'**다. 우리는 기존의 범용 포맷(TOON 등)에 종속되지 않는다. "괄호를 없애고 들여쓰기로 토큰을 압축한다"는 영감만을 차용하여, 백지에서부터 **우리의 시스템에 최적화된 독자적 '형태 언어'**를 창조하고 진화시킨다.

### 3.1. 시맨틱 압축과 기호 논리학 (Symbolic Logic)

모호한 자연어 지시를 철저히 배제하고, 수학적/시각적 기호만으로 제어 흐름을 강제하여 환각을 차단한다. 자연어는 오직 주석(`#`)으로만 허용된다.

* `$` : 글로벌 심볼 참조 및 시맨틱 앵커 (예: `$User.login`)
* `!` : 필수 제약 / 불변 조건 검증 (위반 시 실행 조기 종료)
* `?` : 조건문 분기 (Decision Gate)
* `>` : 함수 실행 및 액션 (Action/Transformation)
* `<-` : 변수 할당
* `*` : 반복 루프
* `=>` / `!=>` : 정상 반환 / 예외 전파 (Err)
* `@` : 데코레이터 및 메타데이터

### 3.2. 언어 작성 예시 (Denavy DSL)

**[정적 DNA: `/dna/secure_core.dna`]**

```text
package com.denavy.security

entity User extends BaseEntity:
    userId: UUID @primary
    email: PII<String> @sensitive
    status: UserStatus = 'ACTIVE'

policy DataProtection:
    enforce encryption('AES-256') on field(@sensitive)

```

**[동적 SPEC: `/spec/auth_flow.spec`]**

```text
import: com.denavy.security.User

$User.login(credentials):
  > sanitizers.clean_input(credentials.email)
  ?! credentials.email.isValidFormat:
    > return Error("INVALID_FORMAT")

  > $User.findByEmail(credentials.email) <- user_record
  ! user_record.status == 'ACTIVE':  # 정책 강제. 위반 시 샌드박스 차단
    > security.trigger_alert("Inactive user")
    > return Error("LOCKED")

```

---

## 4. 진화 생태계: ALE 역설계와 연옥 결재 (Purgatory Approval) 메커니즘

**딜레마:** "원칙은 그렇다 해도, 인간 개발자가 핫픽스가 급해서 `/src` 코드를 IDE에서 손으로 직접 수정해버리면 명세서가 구식이 되지 않는가?"
Denavy는 이 틈새를 무지성 자동 병합의 위험성 없이, **추상 논리 추출(ALE)과 연옥(Purgatory) 결재 엔진**으로 방어한다.

1. **추상 논리 추출(ALE):** 파일 감시자가 `/src`의 수동 변경을 감지하면, 백그라운드의 추상 논리 추출기(ALE)가 띄어쓰기 같은 구문적 노이즈를 무시하고 "새로운 IP 차단 로직 추가"라는 **순수 논리적 변이(Logical Delta)**만 역설계하여 추출한다.
2. **연옥 대기 (Drafting):** 추출된 델타는 원본 명세를 즉시 덮어쓰지 않고, 임시 제안서(`/spec/draft`) 상태로 연옥에 격리(Quarantine)된다. 무지성 병합은 AI 환각과 1:N:M 뼈대 붕괴를 초래하므로 절대 허용하지 않는다.
3. **인간의 절대 결재 (Approve):** 시스템은 인간 아키텍트에게 알림을 띄운다. 오직 인간이 이 역방향 병합안을 검토하고 승인(Approve) 도장을 찍어야만 공식 명세(`/spec`)와 헌법(`/dna`)이 갱신되며 머클 루트가 재계산된다.

---

## 5. 인프라와 무결성: 머클 트리와 일회성 샌드박스

AI가 작성한 신뢰할 수 없는(Untrusted) 코드를 프로덕션 환경에서 안전하게 돌리기 위해, Denavy는 런타임 레벨에서 완벽한 암호학적, 물리적 격리망을 구축한다.

### 5.1. 머클 트리(Merkle Tree) 기반 무결성 저장소

프로젝트의 모든 `.dna`와 `.spec` 파일은 블록체인처럼 잘게 쪼개져 해시화되어 머클 루트(Merkle Root)를 형성한다.

* **부트로더 검증:** 에이전트가 코드를 샌드박스에서 실행하기 전, 런타임 부트로더가 해시값을 대조한다.
* **가드레일:** 악의적인 프롬프트 인젝션이나 스펙 드리프트(Spec Drift)로 인해 명세가 단 1비트라도 변조되었다면 **즉각 실행을 차단(Halt)**하고 경보를 발령한다.

### 5.2. 일회성 샌드박스 (AWS Firecracker)

Docker 컨테이너는 커널 공유로 인한 탈옥 및 'Noisy Neighbor' 위험이 상존한다. Denavy 런타임은 콜드 스타트 125ms, 메모리 오버헤드 5MB에 불과한 **하드웨어 가상화 마이크로VM (Firecracker)**을 기반으로 한다.

* 에이전트가 코드를 짤 때마다 일회성(Ephemeral) 쉐도우 샌드박스(Shadow Sandbox)를 띄워 **사법부(`/spec/test/`) 채점표를 격리 테스트**하고, 로그 결과(stdout/stderr)만 추출한 뒤 VM을 즉시 폭파하여 100% 물리적 보안을 달성한다.

---

## 6. 관측 가능성과 논리적 허점 처리 (Absolute Halt Protocol)

에이전트의 오작동 및 무한 루프를 제어하기 위한 프랙탈 관측 인프라를 가동하되, **에러 치유에 대한 권한은 철저히 이분법적으로 분리**한다.

### 6.1. 프랙탈 관측 (OpenTelemetry 기반)

에이전트의 사고 과정(Chain of Thought)을 OpenTelemetry 표준에 맞춰 DAG(Directed Acyclic Graph) 형태로 시각화한다. 에이전트가 동일한 샌드박스 에러와 상태 해시(State Hash)를 임계값 이상 반복하면 '진전 없는 무한 루프'로 간주하여 샌드박스를 강제 종료시킨다.

### 6.2. 에러 대응의 이분법적 절대 원칙 (바이브 디버깅 금지)

**① 구현 오류 (Implementation Error)**

* **증상:** 오타, 문법 실수, 라이브러리 사용법 오류 등 샌드박스에서 `SyntaxError`, `ImportError` 등이 발생하거나, **사법부(`/spec/test/`) 채점표를 100% 통과하지 못한 경우**.
* **조치:** 샌드박스의 에러 로그(`stderr`)를 보고, 에이전트가 명세를 건드리지 않은 채 `/src` 코드 레벨에서 스스로 코드를 재작성(Syntax Repair)하여 디버깅한다.

**② 명세 오류 (Specification Error) / 논리적 모순 ★ 절대 헌법 ★**

* **증상:** 코드는 에러 없이 실행되고 채점표도 통과(Exit Code 0)했지만 실제 비즈니스 로직 결과가 틀리거나, 무한 루프에 빠지거나, 명세 자체에 설계상 모순이 있어 도저히 사법부를 통과할 수 없는 경우.
* **조치 프로세스:**

1. **[절대 금지] 에이전트는 어떠한 경우에도 스스로 판단하여 명세(`/spec`, `/dna`)를 뜯어고칠 권한이 없다.**
2. 에이전트는 무작정 코드를 고치는 행위를 멈추고 즉시 코딩 및 샌드박스 실행을 **강제 중단(Halt)**한다.
3. 작업 중인 칸반 파일(봉화)의 상태를 `Blocked`로 변경한다.
4. 인간 아키텍트에게 **"명세의 N번 라인에 이러이러한 논리적 모순이 존재하여 진행이 불가합니다. 스펙을 다시 기획하여 주십시오."** 라고 요구(Escalate/Request)한다.
5. 오직 창조주인 인간만이 스펙의 허점을 메우고 결재 도장을 찍어 에이전트를 재가동할 권한을 갖는다.

---
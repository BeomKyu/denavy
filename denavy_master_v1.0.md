# 🧭 [Master Blueprint] Denavy 아키텍처: Spec-First 형태 언어 및 런타임 명세서 v2.0 (Byeorim v1.4 통합본)

> **[System Context for AI Agents]**
> 본 문서는 자율형 AI 코딩 에이전트를 위한 3대 오케스트레이션 축(`Registry`, `Denavy`, `Hwatotbul`) 중, **'공간(디렉토리 트리)과 불변의 법칙(제약/형태언어/런타임)'**을 관장하는 입법부, `Denavy`에 대한 단일 통합 지식 베이스이다. 에이전트는 본 문서를 바탕으로 코드 생성의 물리적 제약, **초정밀 메타 언어 '벼림(Byeorim)'의 문법**, **추상 논리 추출(ALE) 기반의 역방향 결재 메커니즘**, 런타임 무결성 환경을 완벽히 인지해야 한다.

## 0. 아키텍처 진화의 역사 및 애자일 철학 (Historical Context)

에이전트는 이 시스템이 어떠한 고민과 시행착오를 거쳐 현재에 이르렀는지 반드시 인지해야 한다.

* **과거의 환상 (Monolithic & Auto-Healing):**
* 초기에는 Denavy가 에이전트의 상태, 칸반, 트라우마 등 수많은 역할들을 모두 포괄하는 거대한 운영체제로 구상되었다.
* "에이전트가 논리적 허점을 발견하면 스스로 명세를 뜯어고친다"는 자율 치유(Self-Healing)의 환상은 에이전트의 환각을 방치하는 철저한 통제력 상실임을 깨닫고 전면 폐기했다.


* **역방향 동기화(RLS)의 진화 - '무지성 병합'에서 '연옥 결재'로:**
* "인간이 코드를 고치면 시스템이 알아서 `src ➔ spec ➔ dna` 순으로 명세를 덮어씌워 준다"는 것은 우리가 달성해야 할 역방향 선형 동기화(RLS) 로망이다.
* 그러나 역추론 과정에서의 AI 환각 위험, 그리고 지엽적인 소스 코드(`/src`) 수정이 전역 헌법(`/dna`)을 오염시키는 1:N:M 피라미드 붕괴를 방치할 수는 없었다. 따라서 이를 무지성 자동 병합으로 두는 대신, **ALE(추상 논리 추출기)가 역방향 변경안(Draft)만 추출하여 대기시키고, 최종 병합은 반드시 인간의 승인(Approve)을 거치도록 강제하는 '연옥(Purgatory) 결재 메커니즘'으로 안전하게 타협**하고 진화시켰다.


* **현재의 패러다임 (Decoupled & Human-Centric):**
* 수많은 시행착오 끝에 시스템을 3개의 독립된 축으로 철저히 분리했다.


1. **Hwatotbul (시간과 기억):** 트라우마를 제어하는 단일 닻(Anchor) 파일과, 그에 종속된 비동기 칸반 작업장(`Bonghwa`)을 통해 에이전트의 일정을 통제한다.
2. **Registry (명부):** 거대했던 Denavy에서 떼어내어 향후 에이전트 전역 심볼이나 외부 플러그인 등록소 등으로 진화할 여지를 둔다.
3. **Denavy (공간과 법칙):** 본 문서는 오직 '어떤 공간에서, 어떤 언어로 **명세를 기안(Draft) 및 결재(Approve)받고, 런타임에서 증명할 것인가**'에만 집중한다.


* **애자일 진화 선언:**
* 프론트엔드 UI/UX 명세의 완벽한 편입 등은 앞으로 부딪히며 풀어나가야 할 과제이다. 미지의 영역까지 억지로 한 번에 완벽히 설계하려 하기보다, 실제 코딩을 진행하며 한계에 부딪힐 때마다 버전을 업데이트하며 진화시키는 애자일(Agile) 방식을 따른다.



---

## 1. 아키텍처 핵심 철학: Spec-First Paradigm (에이전트의 번역기화)

**"명세가 곧 컴퓨터이다. 에이전트는 명세를 코드로 번역하는 기계(Transpiler)일 뿐이며, 통제권은 오직 인간에게 있다."**

자연어 프롬프트에 의존해 즉흥적으로 코드를 짜는 에이전트의 행위는 유지보수 불가능한 스파게티 코드를 낳는다.

* **코드(`/src`)의 격하:** 실제 소스 코드는 영구적 자산이 아니다. 언제든 지우고 다시 생성할 수 있는 일회성(Ephemeral) 컴파일 찌꺼기다. **당장 명세 자체가 런타임에서 스스로 돌아갈 수는 없기에, 에이전트가 이를 타겟 언어(Java, Python 등)로 기계적으로 번역(Transpile)하는 역할을 수행할 뿐이다.**
* **명세(`/spec`, `/dna`)의 격상:** 시스템의 불변하는 본질과 논리적 흐름은 오직 인간 아키텍트가 결재한 **벼림 명세** 안에만 존재한다. 에이전트는 이 명세를 완벽히 통과할 때만 코드를 배포할 수 있다.
* **기안(Draft)과 결재(Approve)의 분리:** 에이전트는 지시를 받으면 즉시 코드를 짜는 것이 아니라, 반드시 압축된 기호 언어(벼림)로 시스템 3대 명세(DNA/SPEC/TEST) 초안을 스스로 **'기안'**하여 보고할 의무를 진다. 인간 아키텍트의 **'결재'**가 떨어진 후에야 비로소 샌드박스에서 타겟 언어(`/src`)로 코드를 번역하여 구현할 권한을 얻는다.

---

## 2. 공간의 통제: 도메인 기반 1:N:M 피라미드 미러링

프로젝트의 공간은 비즈니스 도메인(예: `auth`, `payment`)을 기준으로 3개의 물리적 최상위 폴더에 엄격히 격리된다. 이는 정적인 뼈대(데이터)와 동적인 흐름(로직)을 섞었을 때 발생하는 LLM 컨텍스트 오염을 막기 위함이다.
파일 구조는 맹목적인 1:1:1 맵핑이 아닌, **`1(DNA) : N(SPEC/TEST) : M(SRC)`의 피라미드 투영(Projection) 구조**를 띤다.

### 2.1. `/dna` (정적 뼈대 및 절대 제약 / 입법부 - "명사와 상태")

시스템의 데이터 뼈대(Entity 구조, 타입)와 시공간을 초월하여 절대 위반해선 안 되는 글로벌 보안/비즈니스 제약(Policy)만 정의한다. 시간의 흐름(순서)이나 제어 로직은 일절 들어가지 않는 철저한 Read-Only 영역이다.

* **계층적 오버라이딩:** 최상위 헌법인 `/dna/root.dna`가 전체 제약을 깔고, 하위 도메인 폴더로 깊이 들어갈수록 로컬 뼈대가 전역 뼈대를 덮어쓰거나(Override) 우선한다.

### 2.2. `/spec` (운영 및 검증 명세 계층 / 행정·사법부 - "동사와 흐름")

`/dna`의 뼈대를 참조하여, 시간의 흐름에 따른 조건 분기, 함수 실행 등의 제어 흐름(Logic)을 **벼림(Byeorim)** 언어로 정의하는 '설계도'다. 인간이 결재하고 에이전트가 코드를 번역할 때 쳐다보는 유일한 진실의 원천이다.

* **사법부의 하위 폴더 분리 (`/spec/test/`):** 파일 비대화를 막기 위해, 로직 명세(`.spec`)와 검증 채점표(`.test.spec`)는 물리적으로 엄격히 분리한다. 에이전트는 이 사법부 채점표를 100% 통과하는 코드를 짜야만 한다.

### 2.3. `/src` (구현체 계층 / 결과물)

벼림 명세들을 바탕으로 에이전트가 기계적으로 찍어낸 타겟 언어 소스 코드. 인간은 이 폴더를 직접 수정하지 않는 것을 원칙으로 한다. 에이전트는 벼림 명세를 해석하여, 타겟 프레임워크(Spring Boot, Express 등)의 관례에 맞추어 자율적으로 코드를 빚어낼(Transpile) 책임을 진다.

---

## 3. 언어의 통제: 초정밀 메타 청사진 언어 『벼림(Byeorim) v1.4』

LLM에게 기존 프로그래밍 언어나 JSON/YAML은 괄호(`{}`), 따옴표(`""`), 쉼표(`,`)가 난무하는 **'구문적 노이즈(Syntactic Noise)'**다. 에이전트가 `/src` 코드를 짜기 전 거쳐야 하는 진실의 원천(`/spec`)은, 오직 인간의 비즈니스 논리만을 1%의 정보 손실 없이 꽂아 넣기 위해 창조된 형태학적 메타 언어 **'벼림(Byeorim)'**으로 작성된다.

### 3.1. 벼림(Byeorim) 3대 설계 헌법

1. **Zero Noise (오컴의 쳐내기):** 중괄호, 쉼표, 따옴표 전면 불법화. 공간과 계층은 오직 '들여쓰기'로 증명하며, 자연어는 언더바(`_`)로 맨몸 연결한다. (단, 캡슐화용 소괄호 `()`는 합법)
2. **Pragmatic Frictionless (구조와 수식의 분리):** 시스템을 제어하는 거시 뼈대는 벼림의 룬으로 엄격히 통제하되, 순수 내부 논리 연산(`!=`, `<`, `==`)은 보편적 수학 기호를 합법화하여 인지 마찰을 '0'으로 수렴시킨다.
3. **3대 형태학 계급:** 에이전트가 스캔만 해도 로직의 무게를 깨닫도록 기호를 3계급으로 철저히 분리한다.
* 🅰️ **[거시 통제와 공간]** : `대문자 3~4글자 룬` (`MUST`, `TX`, `FAIL`, `ST`, `PUB` 등) - 위반 시 샌드박스를 폭파하거나 거대한 결계를 창조.
* 🅱️ **[미시 실행과 흐름]** : 방향성을 가진 `화살표` (`<-`, `/>`, `|>`, `=>`, `?=>` 등) - 데이터와 로직의 물리적 이동.
* Ⓒ **[연결과 닻]** : 특수기호 또는 `소문자` (`$`, `=`, `:`, `use` 등) - 조용한 내부 할당 및 식별자.



### 3.2. 언어 작성 및 AI 번역(Transpiling) 예시

인간 아키텍트가 아래와 같이 **벼림 명세서**를 작성하여 결재하면, 에이전트는 이를 분석하여 목표 프레임워크(Spring Boot, Node.js 등)의 보일러플레이트를 입힌 완벽한 `.src` 코드로 **기계적 번역(Transpile)**을 수행할 의무를 진다.

**[정적 DNA: `/dna/auth/secure.dna`]**
*시간의 흐름이 없는 순수 명사와 제약(Policy)의 뼈대 공간. 에이전트는 이를 읽고 엔티티/모델 클래스(`/src`)를 생성한다.*

```text
NOW 2026-03-02
BAN 유저_비밀번호_평문_저장_절대_금지

PUB $Security.User :: $BaseEntity:
  ST userId: UUID
  ST email: String
  ST status: String = ACTIVE

```

**[동적 SPEC: `/spec/auth/login.spec`]**
*에이전트는 이 파일을 읽고 실제 동작하는 타겟 언어의 비즈니스 로직(Controller/Service)을 기계적으로 찍어낸다.*

```text
use $Security.User

PUB $Auth.login(credentials):
  # 💡 [파이프라인] 자연스러운 데이터 연쇄 가공
  cleaned_email = credentials.email |> sanitize() |> to_lower()

  # 💡 [예외 반환] 보편 수식(!=)과 예외 룬(?=>)의 마찰 없는 결합
  ?=> cleaned_email.isValid() != true:
    => Error(잘못된_이메일_형식)

  # 💡 [외부 견인] DB에서 데이터를 무겁게 당겨옴(<-)
  user_record <- $DB.User.findByEmail(cleaned_email)
  
  # 💡 [절대 제약] 에이전트는 이 조건을 위반하면 예외를 던지도록 코드를 짜야 함
  MUST user_record.status == ACTIVE

  # 💡 [트랜잭션 결계 & 재난 수습] 에이전트가 알아서 롤백 코드를 생성
  TX $Auth.generate_token:
    token <- $JWT.sign(user_record)
    /> $Log.audit_login(user_record)       # 비동기 발사 (기다리지 않음)
    
  FAIL err:
    /> $Log.error(err.msg)
    => Error(토큰_발급_실패)

  => token                                 # 정상 반환 (=>)

```

---

## 4. 진화 생태계: ALE 역설계와 연옥 결재 (Purgatory Approval) 메커니즘

**딜레마:** "원칙은 그렇다 해도, 인간 개발자가 핫픽스가 급해서 `/src` 코드를 IDE에서 손으로 직접 수정해버리면 명세서가 구식이 되지 않는가?"
Denavy는 이 틈새를 무지성 자동 병합의 위험성 없이, **추상 논리 추출(ALE)과 연옥(Purgatory) 결재 엔진**으로 방어한다.

1. **추상 논리 추출(ALE):** 파일 감시자가 `/src`의 수동 변경을 감지하면, 백그라운드의 추상 논리 추출기(ALE)가 띄어쓰기 같은 구문적 노이즈를 무시하고 순수 논리적 변이(Logical Delta)만 역설계하여 **벼림(Byeorim) 문법으로 추출**한다.
2. **연옥 대기 (Drafting):** 추출된 델타는 원본 명세를 즉시 덮어쓰지 않고, 임시 제안서(`/spec/draft`) 상태로 연옥에 격리(Quarantine)된다. 무지성 병합은 AI 환각과 1:N:M 뼈대 붕괴를 초래하므로 절대 허용하지 않는다.
3. **인간의 절대 결재 (Approve):** 시스템은 인간 아키텍트에게 알림을 띄운다. 오직 인간이 이 역방향 병합안을 검토하고 승인(Approve) 도장을 찍어야만 공식 벼림 명세(`/spec`)와 헌법(`/dna`)이 갱신되며 머클 루트가 재계산된다.

---

## 5. 인프라와 무결성: 머클 트리와 일회성 샌드박스

AI가 작성한 신뢰할 수 없는(Untrusted) 코드를 프로덕션 환경에서 안전하게 돌리기 위해, Denavy는 런타임 레벨에서 완벽한 암호학적, 물리적 격리망을 구축한다.

### 5.1. 머클 트리(Merkle Tree) 기반 무결성 저장소

프로젝트의 모든 `.dna`와 `.spec` 파일은 블록체인처럼 잘게 쪼개져 해시화되어 머클 루트(Merkle Root)를 형성한다.

* **부트로더 검증:** 에이전트가 코드를 샌드박스에서 실행하기 전, 런타임 부트로더가 해시값을 대조한다.
* **가드레일:** 악의적인 프롬프트 인젝션이나 스펙 드리프트(Spec Drift)로 인해 명세가 단 1비트라도 변조되었다면 **즉각 실행을 차단(Halt)**하고 경보를 발령한다.

### 5.2. 일회성 샌드박스 (AWS Firecracker)

Docker 컨테이너는 커널 공유로 인한 탈옥 및 'Noisy Neighbor' 위험이 상존한다. Denavy 런타임은 콜드 스타트 125ms, 메모리 오버헤드 5MB에 불과한 **하드웨어 가상화 마이크로VM (Firecracker)**을 기반으로 한다.

* 에이전트가 코드를 짤 때마다 일회성(Ephemeral) 쉐도우 샌드박스를 띄워 **사법부(`/spec/test/`) 채점표를 격리 테스트**하고, 로그 결과(stdout/stderr)만 추출한 뒤 VM을 즉시 폭파하여 100% 물리적 보안을 달성한다.

---

## 6. 관측 가능성과 논리적 허점 처리 (Absolute Halt Protocol)

에이전트의 오작동 및 무한 루프를 제어하기 위한 프랙탈 관측 인프라를 가동하되, **에러 치유에 대한 권한은 철저히 이분법적으로 분리**한다.

### 6.1. 프랙탈 관측 (OpenTelemetry 기반)

에이전트의 사고 과정(Chain of Thought)을 OpenTelemetry 표준에 맞춰 시각화한다. 에이전트가 동일한 샌드박스 에러와 상태 해시(State Hash)를 임계값 이상 반복하면 '진전 없는 무한 루프'로 간주하여 샌드박스를 강제 종료시킨다.

### 6.2. 에러 대응의 이분법적 절대 원칙 (바이브 디버깅 금지)

**① 구현 오류 (Implementation Error)**

* **증상:** 오타, 문법 실수, 라이브러리 사용법 오류 등 `/src` 샌드박스에서 `SyntaxError`가 발생하거나, **사법부(`/spec/test/`) 채점표를 100% 통과하지 못한 경우**.
* **조치:** 에이전트가 벼림 명세를 건드리지 않은 채 `/src` 코드 레벨에서 스스로 타겟 언어의 코드를 재작성(Syntax Repair)하여 디버깅한다.

**② 명세 오류 (Specification Error) / 논리적 모순 ★ 절대 헌법 ★**

* **증상:** 코드는 에러 없이 실행되고 채점표도 통과했지만 실제 비즈니스 로직 결과가 틀리거나, 벼림 명세(MUST 등) 자체에 설계상 모순이 있어 도저히 사법부를 통과할 수 없는 경우.
* **조치 프로세스:**
1. **[절대 금지] 에이전트는 어떠한 경우에도 스스로 판단하여 명세(`/spec`, `/dna`)를 뜯어고칠 권한이 없다.**
2. 에이전트는 무작정 코드를 고치는 행위를 멈추고 즉시 코딩 및 샌드박스 실행을 **강제 중단(Halt)**한다.
3. 작업 중인 칸반 파일(봉화)의 상태를 `Blocked`로 변경한다.
4. 인간 아키텍트에게 **"벼림 명세의 N번 라인에 이러이러한 논리적 모순이 존재하여 진행이 불가합니다. 스펙을 다시 기획하여 주십시오."** 라고 요구(Escalate)한다.
5. 오직 창조주인 인간만이 벼림 헌법의 허점을 메우고 결재 도장을 찍어 에이전트를 재가동할 권한을 갖는다.